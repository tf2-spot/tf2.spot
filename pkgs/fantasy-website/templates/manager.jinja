{% extends "base.jinja" %}

{% block title %}
  {% if manager.name is none %}
    Manager #{{ manager.steam_id }}
  {% else %}
    {{ manager.name }}
  {% endif %}
{% endblock title %}

{% block main %}
  <pre>{{ manager | tojson(2) }}</pre>
{% endblock main %}
